# Skill template

Copy this folder to `config/skills/your_skill_name/` and fill in files.

Human review required before setting `engines.*.status: active` in `platform.yaml`.

See [docs/SKILL_AUTHORING.md](../../docs/SKILL_AUTHORING.md).
