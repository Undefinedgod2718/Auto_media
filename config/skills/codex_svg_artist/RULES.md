# SVG rules

- `width="1080"` `height="1080"` `viewBox="0 0 1080 1080"`.
- No external `href`, fonts, or images.
- Use system sans-serif via `font-family="sans-serif"` only.
- Self-contained single root `<svg>` element.
- No scripts or animations (static post image).
