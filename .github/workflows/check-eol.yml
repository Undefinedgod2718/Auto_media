name: Check EOL

on:
  push:
  pull_request:

jobs:
  check-eol:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Enforce LF line endings
        run: CHECK_EOL_STRICT=1 bash scripts/check_eol.sh
