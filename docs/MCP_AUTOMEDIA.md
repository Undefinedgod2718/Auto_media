# Auto Media MCP

`automedia-mcp` exposes a small MCP stdio server for external LLM hosts.

Server script:

- `scripts/mcp_automedia.py`

Config template:

- `config/mcp/automedia.json`

## Tools

- `run_get_status` (read run `state.json`)
- `run_require_stage` (check `stage_seq` threshold)
- `run_mark_stage` (write-enabled only)
- `doctor` (runs `scripts/env-check.sh`)

## Safety

Write operations are disabled by default.  
Enable only when needed:

```bash
AUTO_MEDIA_MCP_WRITE=1 python3 scripts/mcp_automedia.py
```

## Integration

| Client | Config | Notes |
|--------|--------|-------|
| **Cursor / Dev Container** | [`config/mcp/automedia.json`](../config/mcp/automedia.json) | Repo paths; commit-friendly |
| **Hermes (Linux host)** | `config/hermes/mcp.json` | Generated by `amctl apply`; uses `paths.repo_root_host` + `{repo}/data` |
| **n8n debug** | [`.mcp.json.example`](../.mcp.json.example) → local `.mcp.json` | See [N8N_MCP.md](N8N_MCP.md) — separate server |
| **Operator UI** | Dashboard `:8790` | Not MCP — tokens/skills/engines |

For Cursor, point MCP at `config/mcp/automedia.json` (or copy and fix absolute paths on another machine).

## Bridges (do not merge)

- **automedia** — `state.json` / stage gate / `doctor`
- **n8n-mcp** — n8n REST (Cursor only)
- **playwright** — in Hermes `mcp.json` when `publish.mode` is `auto` or `dom`
- **Dashboard** — HTTP localhost; `AUTO_MEDIA_DASHBOARD_*` env, not `AUTO_MEDIA_MCP_WRITE`

## Empirical verification (E1–E7)

Automated (repo):

```bash
bash scripts/verify_mcp_evidence.sh
```

| ID | Check | Pass criterion |
|----|-------|----------------|
| E1 | Hermes `mcp.json` paths | `args` → `{repo}/scripts/mcp_automedia.py`; `DATA_ROOT` → `{repo}/data`; not `/data/scripts` |
| E2 | automedia stdio | `initialize` returns `result`; optional `run_get_status` on existing `data/runs/<id>` |
| E3 | n8n reachability | `n8n_api_url_resolve_reachable` + `/healthz`; optional workflows API if `N8N_API_KEY` in `.env` |
| E4 | Dashboard | `:8790/healthz` when `open_user_ui.sh` running (warn if down) |
| E5 | playwright in Hermes mcp | Present when `publish.mode` ∈ `auto`,`dom` |
| E7 | `capture_n8n_logs.sh` | Manual: `--execution-id` + `--run-id` → `data/logs/n8n-mcp/capture-*/summary.json` |

**Manual (UNVERIFIED in CI):**

| ID | Check | Doc |
|----|-------|-----|
| E3f | Cursor `npx n8n-mcp` handshake | [N8N_MCP.md](N8N_MCP.md) § Manual verification |
| E6 | Hermes runtime loads `mcp.json` | [N8N_MCP.md](N8N_MCP.md) § Manual verification |

Evidence snapshot (2026-06-03 devcontainer, post-fix): E1 Hermes repo paths **PASS** (`verify_mcp_evidence.sh`); E2 automedia **PASS**; E3 — `localhost:5678` **FAIL** in container, use `host.docker.internal` in [`.mcp.json.example`](../.mcp.json.example); E3f/E6 **UNVERIFIED** (Cursor/Hermes manual). After `docker compose build`: run [`scripts/post_docker_rebuild.sh`](../scripts/post_docker_rebuild.sh).

## Related

- [VERIFY.md](VERIFY.md) — layer I MCP evidence
- [N8N_MCP.md](N8N_MCP.md) — Cursor n8n-mcp template
- [HERMES_SETUP.md](HERMES_SETUP.md) — host Hermes + data mount
